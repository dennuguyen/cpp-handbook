// Output true if m1 is equal to the inverse of m2.
// A map is inverted if the values and keys are swapped.
// Use STL algorithms only. No for loops.

#include <ios>
#include <iostream>
#include <map>

bool is_inverted_maps_equal(std::map<int, int>& m1, std::map<int, int>& m2) {
    // WRITE YOUR SOLUTION HERE.
    return false;
}
